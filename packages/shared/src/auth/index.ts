// Auth SDK — placeholder for Phase 2.
//
// When s-authn is ported to the platform, this package will export:
//   - JWT verification utilities (jwtVerify with JWKS cache)
//   - Lambda authorizer handler (shared across modules)
//   - Hono middleware: authMiddleware(), requirePermission(), requireSelfOrPermission(), requireSystem()
//   - Token cache (in-memory per Lambda container)
//
// See s-architecture/docs/04-authentication-and-authorization.md.

export {};
