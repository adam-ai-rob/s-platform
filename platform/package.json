{
  "name": "@s/platform-infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tier-1 SST app — API Gateway v2, EventBridge bus + archive, KMS JWT signing key, SNS alarms topic, DNS + cert. Publishes outputs to SSM /s-platform/{stage}/* for module SST apps to consume.",
  "scripts": {
    "dev": "sst dev",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "dependencies": {
    "@pulumi/aws": "^7.0.0",
    "@pulumi/pulumi": "^3.200.0",
    "@s/infra-shared": "workspace:*"
  },
  "devDependencies": {
    "sst": "^3.0.0"
  }
}
