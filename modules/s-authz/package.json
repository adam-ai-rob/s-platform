{
  "name": "@s/module-s-authz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tier-2 SST app for the s-authz module — tables, Lambdas, stream + event handlers. Reads platform ARNs from SSM, registers its /authz/* route against the platform-owned API Gateway.",
  "scripts": {
    "dev": "sst dev",
    "deploy": "sst deploy",
    "remove": "sst remove"
  },
  "dependencies": {
    "@pulumi/aws": "^7.0.0",
    "@pulumi/pulumi": "^3.200.0",
    "@s/infra-shared": "workspace:*"
  },
  "devDependencies": {
    "sst": "^3.0.0"
  }
}
